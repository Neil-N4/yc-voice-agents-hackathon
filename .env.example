# Copy to .env and fill in. Never commit .env.

# Cactus API token (from cactuscompute.com/dashboard/api-keys)
CACTUS_TOKEN=

# Gemini cloud fallback (from aistudio.google.com/api-keys)
# Used only when on-device E4B struggles on reconciliation.
GEMINI_API_KEY=
